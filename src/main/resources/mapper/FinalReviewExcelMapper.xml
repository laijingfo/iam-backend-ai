<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lenovo.mapper.FinalReviewExcelFileMapper">


    <delete id="clearFinalReviewExcelFile">
        truncate table final_review_excel_file;
    </delete>

    <select id="selectAllExcel" resultType="java.util.Map">
        SELECT DISTINCT ON (LOWER(TRIM(it_code)))
            LOWER(TRIM(it_code)) AS it_code,
            file_content
        FROM final_review_excel_file
        WHERE it_code IS NOT NULL
          AND TRIM(it_code) != ''
        ORDER BY
            LOWER(TRIM(it_code)),
            create_time DESC NULLS LAST,
            id DESC
    </select>
</mapper>
